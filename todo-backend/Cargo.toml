[package]
name = "todo-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.8"
serde = "1.0.228"
tokio = { version = "1.50.0", features = ["macros", "rt-multi-thread"] }
sqlx = { version = "0.8", features = ["chrono", "macros", "mysql", "runtime-tokio"] }
dotenv = "0.15.0"